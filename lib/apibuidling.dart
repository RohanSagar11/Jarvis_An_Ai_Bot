void main() {
  // var username = "RohanSagar123";
  // String password = "rohan@token";
  // String decode = "$username" + ":" + "$password";
  // final bytes = utf8.encode(decode);
  // String encode = base64Encode(bytes);
  // print(encode);
  // print(DateTime.now());
  String header = "{\"type:\"" + "JWT" + "hashalgo"+":" + "hash"+"}";
  print(header);
}
